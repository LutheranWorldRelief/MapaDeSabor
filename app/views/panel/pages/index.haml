%br
.row
  .small-12.medium-12.large-12
    .callout.secondary
      .row
        .medium-9.columns
          %h4= model.model_name.human(count: :many)
        .medium-3.columns
          - if @items.blank?
            = link_to new_item_url, class: 'primary button no-margin float-right' do
              %span.glyphicon.glyphicon-plus
              Nuevo
    %table.table.table-hover.table-striped.simple-data-table
      %thead
        %tr
          %th{ width: '50%' } Titulo de la página
          %th{ width: '30%' } Última modificación
          %th{ widht: '20%' }
      %tbody
        -if @items
          - @items.each do |item|
            %tr
              %td
                = item.title
              %td
                = item.updated_at.strftime("%d/%m/%Y %H:%M")
              %td
                = link_to "Editar", URI.decode(edit_item_url(item.id)), class: "button"
